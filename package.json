{
  "name": "cdda-customnpc",
  "version": "1.0.0",
  "description": "快速定义cdda的随从npc",
  "main": "index.js",
  "scripts": {
    "build-schema": "node ./dist/BuildSchema.js",
    "compile": "tsc && tsc-alias",
    "build": "npm run compile && node --trace-warnings index"
  },
  "author": "zwa73",
  "license": "ISC",
  "dependencies": {
    "@zwa73/utils": "^1.0.42",
    "cdda-schema": "file:CDDA-Schema",
    "typescript": "^5.2.2",
    "typescript-json-schema": "^0.61.0"
  }
}
